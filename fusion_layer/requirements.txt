# Core dependencies for local-only fusion layer
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0
PyYAML>=6.0.0
pytest>=7.0.0

# Optional: only needed if using S3 integration (not required for local file deployment)
# boto3>=1.34.0
